export enum EnumUserUrl {
	PROFILE = '/profile',
	FAVORITE = '/profile/favorites',
	BY_ID = '/by-id'
}

export enum EnumGenreUrl {
	BY_ID = '/by-id',
	BY_SLUG = '/by-slug'
}

export enum EnumActorUrl {
	BY_ID = '/by-id',
	BY_SLUG = '/by-slug'
}

export enum EnumReviewUrl {
	LEAVE = '/leave'
}

export enum EnumStatisticsUrl {
	MAIN = '/main',
	MIDDLE = '/middle'
}

export enum EnumMovieUrl {
	MOST_POPULAR = '/most-popular',
	BY_ACTOR = '/by-actor',
	BY_GENRES = '/by-genres',
	UPDATE_VIEWS = '/update-count-views',
	BY_ID = '/by-id',
	BY_SLUG = '/by-slug'
}

export enum EnumAuthUrl {
	ACCESS_TOKEN = '/login/access-token'
}
